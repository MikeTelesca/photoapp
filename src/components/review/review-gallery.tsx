"use client";

// Placeholder during strip-to-core. The rebuilt review UI lives in
// /job/[jobId]/page.tsx and its child components. This stub exists only
// so any remaining import resolves while the transition is in progress.

export default function ReviewGallery(): null {
  return null;
}
